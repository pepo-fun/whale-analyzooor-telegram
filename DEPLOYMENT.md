# Telegram Bot Environment Setup

This bot requires the following environment variables:

- TELEGRAM_BOT_TOKEN: Your bot token from @BotFather
- DATABASE_URL: Railway PostgreSQL connection string
- WHALE_API_URL: API endpoint for whale data
- POLLING_INTERVAL: Check interval in seconds (default: 10)

Make sure to set these in your Railway deployment environment variables.
